server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tauabrandao/tmg-configurations
          default-label: main
          search-paths: 'message-generator*'
